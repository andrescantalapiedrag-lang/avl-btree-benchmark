AVL vs B-Tree Benchmark (C++)

This project provides an experimental comparison between an AVL tree and B-trees of varying order m.

The codebase is implemented in C++ and is designed to be reproducible via Docker.
